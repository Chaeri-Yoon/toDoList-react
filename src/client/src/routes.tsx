const MAIN = '/';
const API = '/api';
const LOGIN = '/login';
const LOGOUT = "/logout";
const JOIN = '/join';
const ADDTASK = '/add-task';
const EDITTASK = '/edit-task';
const DELETETASKAPI = '/delete-task';

const routes = {
    main: MAIN,
    api: API,
    login: LOGIN,
    logout: LOGOUT,
    join: JOIN,
    addTask: ADDTASK,
    editTask: EDITTASK,
    deleteTask: DELETETASKAPI
}
export default routes;